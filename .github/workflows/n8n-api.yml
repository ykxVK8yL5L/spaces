name: 使用抱脸SDK创建青龙面板
on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      Key:
        description: 'N8N的加密Key'
        required: true
        default: ''
      UserID:
        description: '抱脸用户名'
        required: true
        default: ''
      Image:
        description: 'Docker镜像地址'
        required: false
        default: ''

jobs:
  build:
    runs-on: macos-latest
    if: github.event.repository.owner.id == github.event.sender.id
    steps:
      - uses: actions/checkout@v2
        with:
          persist-credentials: true # otherwise, the token used is the GITHUB_TOKEN, instead of your personal token
          fetch-depth: 0 
          
      - name: 隐藏输入信息
        uses: levibostian/action-hide-sensitive-inputs@v1
      
      - name: 安装Rclone
        run: |
            curl https://rclone.org/install.sh | sudo bash

      - name: 检查系统依赖
        run: |
            rclone -V
            python -V
            
      - name: 配置Rclone
        run: |
          printf "$RCLONE_ENV_FILE" >> ~/.config/rclone/rclone.conf
        shell: bash
        env:
          RCLONE_ENV_FILE : ${{secrets.RCLONE_CONF}}
        
      - name: 安装所需依赖
        run: |
          python -m pip install --upgrade pip wheel
          pip install huggingface_hub

      - name: 运行脚本
        run: |
          python n8n.py --token="${{ github.event.inputs.Token}}" --userid="${{ github.event.inputs.UserID}}" --key="${{ github.event.inputs.Key}}" --image="${{ github.event.inputs.Image}}"
   
      # - name: Delete workflow runs
      #   uses: Mattraks/delete-workflow-runs@v1.2.3
      #   with:
      #     token: ${{ github.token }}
      #     repository: ${{ github.repository }}
      #     retain_days: 0
      #     keep_minimum_runs: 0
